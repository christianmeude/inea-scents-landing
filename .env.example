# Landing Page env contract — names only, never real values.
# Local: copy to .env.local with VITE_API_URL=http://127.0.0.1:8000
# Vercel (production landing project): VITE_API_URL=https://inea-scents.onrender.com
VITE_API_URL=
# Mobile App base URL for the "Book in App" button (code appends /#/login
# unless the value already contains a hash). Flutter web uses ephemeral
# local ports, so paste your current one, e.g. http://localhost:62409
# Vercel (production): VITE_FRONTEND_URL=https://inea-scents.vercel.app
VITE_FRONTEND_URL=
